%% ASP Program for Player 0

{ move(1, Dir, nil) : direction(Dir, _,_) } = 1.
{ surface(2) }.

moveIntoCell(NewR, NewC) :- myPos(R, C), move(_, Dir, _), direction(Dir, DR,DC), NewR = R + DR, NewC = C + DC.
water(R, C) :- waterCell(R, C, S).

:- moveIntoCell(R, C), not water(R, C).
:- moveIntoCell(R, C), visitedCell(R, C).

:~ surface(T). [1@1, T]